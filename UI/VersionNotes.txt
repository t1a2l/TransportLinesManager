<extended><color #9999FF>TLM 14.5.3 - 05/APR/24</color>
	- Fix intercity bus line bug with two way outside connection roads.

<color #9999FF>TLM 14.5.2 - 13/MAR/24</color>
	- Added a soft despanw option to buses - will drop of passengers and go back to the depot.

<color #9999FF>TLM 14.5.1 - 11/JAN/24</color>
	- UI Resolution error fix.
	- Unscaled bug fix when there are less then 4 stops.
	- Depot search support.
	- Depot and Asset lowercase search support.
	- Consistancy in all translations.
	- Fix regional buses bug.
	- Now Support the Train-Bus Hub regional lines.
	- Attempt to fix reginal trains bug.
	
<color #9999FF>TLM 14.5 - 22/JUL/23</color> 
	- Treasure Hunt Updated.
	- Now using harmony 2.2.
	- Fix checkboxes not saving state.

<color #9999FF>TLM 14.5 - 30/JUL/23</color>
	- Bus will only return to depot from terminal stop.
	- Support Old version Saves.
	- Fix the linear map.
	- Intercity bus terminals will now show regional lines.

<color #9999FF>TLM 14.6 - 17/JUL/26</color>
	- Control vehicle percentage for prefix lines or total count for custom lines for each selected asset of the line.
	- Show actual used vehicles in the line info panel in the current budget hour.
	- SchoolBus mod support with and without the RealTime mod, one click plug and play.
	- Add weekend profile to support RealTime mod to have different budget and ticket prices for weekends.
	- Add near lines to show lines near buildings. if it is a transport asset it will show how many people are waiting in each stop.

<color #9999FF>TLM 14.7 - 21/AUG/26</color>
	- Added a global option to hide the weekend-profile info-bar checkbox for players who do not use the Real Time mod.
	- Added an Auto-spawn compatible vehicles global option: when no vehicles are selected, TLM can choose randomly from compatible vehicle types.
	- Added per-save version tracking so data migrations run only when a save is loaded from an older version.
	- Added an option to automatically rebalance vehicle counts for lines using absolute vehicle counts.
	- Integrated TLM’s internal spawn-delay system. Disable Algernon’s Transit Vehicle Spawn Delay mod when using it. New fairness options are available per depot and per line.
	- Improved vehicle selection and management, using a UI based on Algernon’s Vehicle Selector mod.
	- Added separate vehicle selections for individual regional/outside connections. Each enabled station-platform-to-outside-connection route can now have its own selected intercity vehicles instead of sharing one list with every regional connection of the same transport type.


